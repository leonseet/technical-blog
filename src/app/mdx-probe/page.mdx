# MDX pipeline probe

<div className="border-input bg-primary text-primary-foreground ring-ring">
  Semantic color probe
</div>

| Feature | Status |
| --- | --- |
| GitHub Flavored Markdown | ~~disabled~~ enabled |

```ts title="probe.ts" {1}
const pipeline = "ready";
```
