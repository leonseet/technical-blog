Invoke `/explainer-html` once for the complete `{{BASE_BRANCH}}...{{RUN_TIP}}` diff.
Write exactly `{{PATH}}`. Do not commit it — the harness uploads it after the run.
Do not revise it afterwards.

End with `<promise>COMPLETE</promise>`.
